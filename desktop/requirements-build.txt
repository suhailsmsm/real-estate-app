# Build-time requirements for the Windows desktop app (GitHub Actions).
# The runtime dependencies come from desktop/pyproject.toml; install this file
# AFTER `pip install ../packages/dxb-core ../api ../mcp ../copilot -e ../desktop`
# (pip does not accept relative path lines inside a requirements file portably).
pyinstaller>=6.0
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27
ruff>=0.5
