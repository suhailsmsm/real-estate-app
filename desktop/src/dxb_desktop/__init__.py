"""Real Estate App New — the desktop packaging of the Dubai analytics stack.

One loopback origin serving the real React SPA, the real read-only API
(over an exported SQLite snapshot instead of Postgres), the real MCP server
and the real copilot. See README.md in this folder.
"""

__version__ = "1.0.0"
